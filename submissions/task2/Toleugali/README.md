1) Нашей задачей является анализ временного ряда и предсказание будущих результатов с помощью python и модуля statsmodels. Выполнены все 3 части задания:
a) проверка исходного ряда на стационарность, причем обоими способами: тестом Дики-Фуллера, используя функцию из statsmodels- adfuller, построением скользящих статистик. Вывод: ряд не является стационарным.
б) разложение временного ряда на тренд, сезональность и остаток. Используем функцию seasonal_decompose, а также interpolate, т.к. могут быть пропущены значения. Далее, стандартная визуализация с помощью matplotlib и проверка каждого по-отдельности на стационарность. Вывод: тренд- не стационарен, сезональность и остаток - стационарны
в) Приводим ряд к стационарному. Проверяем является ли временный ряд интегрированным. Так как первые разности ряда стационарны, то он называется интегрированным рядом первого порядка. Впервую очередь, логарифмируем ряд, избавляясь таким образом от тренда.
Находим параметры для построения модели ARIMA: d - порядок интегрированности, уже найден, p найдем из функции частичной автокорреляции(PACF), q из функции автокорреляции(ACF). Делаем это аналитически оценивая значение лагов на графиках.
Далее применяем модель AR, MA и комбинированную AR(I)MA, выводя ошибку RSS. С помощью информационного критерия Акаике находим наилучшую модель: выбираем модель с минимальным значением.
Далее, так как мы работали с измененным рядом, приводим его к исходному scale. Строим модель ARIMA для обучающей выборки, получая r2_score = 0.95( что является хорошим результатом)
Теперь, прогнозируем для тестовой выборки в моделях AR, MA, ARIMA, получаем r2_score для каждого.
Вывод: Для обучающей выборки модель AR строит лучший прогноз. Для тестовой выборки данные модели строят похожие друг на друга плохие прогнозы. r2_score < 0, то есть модели работают хуже горизонтальной линии y=0. Можно сделать вывод, что в ARIMA полезным было бы учитывать сезонность, получая модель SARIMA, исключая тренд. 

2) Для написания программы использовали дистрибутив Anaconda. Реализацию и запуск кода осуществляли в Jupyter Notebook. Для того чтобы его открыть в консоли терминала вводим команду jupyter notebook. 

3) Задание выполнил: Толеугали Мадияр
